# Security policy

This is a portfolio project, not a supported product. Please report security issues by opening a private
security advisory on GitHub (Security tab, "Report a vulnerability").

Never commit API keys. `.streamlit/secrets.toml` and `.env` are git-ignored; use Streamlit's secrets manager.
